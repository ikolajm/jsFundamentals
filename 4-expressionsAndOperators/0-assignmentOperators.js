// Assignment Operators

    // Assignment Operator
    let x = 'whatever';

    // Addition Operator
    let x = (x + 1); //OR let x += 1;

    //Subtraction Operator

    let x = (x - 1) // OR let x-= 1

    // Multiplication Operator
    let x = (x * 1) // OR let x *= 1

    // Division Operator
    let x = (x / 1) // OR let x /= 1

    // Modulus Operator
    let x = (x % 1) // let x %= 1