// result ? true : false

// let x = 6;

// (x > 0) ? console.log("The number is bigger than zero") : console.log("The number is smaller than or equal to zero");

// Challenege - redo age challenge but do them as ternaries

let age = 16;

(age >=25) ? console.log("You can rent a car") : (age < 25 && age >= 21) ? console.log("You can drink") : (age < 21 && age >= 18) ? console.log("You can vote") : console.log("You can't do anything");
